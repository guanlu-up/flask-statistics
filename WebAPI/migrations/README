Single-database configuration for Flask.

---------step---------
# terminal command #

# 初始化迁移文件; 如同 git init
>>> flask db init
# 将模型添加到迁移文件(在项目的数据库结构发生改变时)
>>> flask db migrate
# 迁移文件中的模型映射到数据库中(在数据库中更新结构)
>>> flask db upgrade